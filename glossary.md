# 📖 용어 사전

레슨에 나오는 모델·학습 관련 용어를 한곳에 모았습니다. 위 검색창에 한글이나 영어로 입력하면 걸러집니다. 괄호 안 링크는 그 용어를 직접 다뤄보는 레슨입니다. 레슨 본문에서 점선 밑줄이 있는 단어에 마우스를 올리거나 터치해도 같은 설명이 나옵니다.

## 기본 개념

**모델 (model)** — 입력을 받아 출력을 내는 함수. 딥러닝에서는 "구조(코드) + 파라미터(숫자들)"의 묶음입니다. ([첫 신경망](#21-mlp-mnist))

**파라미터 / 가중치 (parameter / weight)** — 모델 안의 학습되는 숫자들. "7B 모델"은 파라미터가 70억 개라는 뜻입니다. 편향(bias)도 파라미터의 일종입니다. ([텐서와 자동미분](#20-tensor-autograd))

**하이퍼파라미터 (hyperparameter)** — 학습으로 정해지지 않고 사람이 정하는 값. 학습률, 배치 크기, 층 수, 에폭 수 등.

**텐서 (tensor)** — 숫자가 담긴 다차원 배열. 스칼라(0차원), 벡터(1차원), 행렬(2차원)의 일반화. ([텐서와 자동미분](#20-tensor-autograd))

**shape** — 텐서의 각 축 크기. `[64, 1, 28, 28]`은 "이미지 64장, 채널 1, 28×28 픽셀". 딥러닝 버그의 절반은 shape 불일치입니다.

**학습 / 훈련 (training)** — 손실이 줄어들도록 파라미터를 조금씩 고쳐나가는 과정.

**추론 (inference)** — 학습이 끝난 모델을 사용만 하는 것. 파라미터가 바뀌지 않습니다.

**데이터셋 (dataset)** — 학습에 쓰는 (입력, 정답) 쌍들의 모음. 보통 train / validation / test로 나눕니다.

**train / validation / test split** — 학습용 / 학습 중 점검용 / 최종 평가용 데이터. 모델이 본 적 없는 데이터로 평가해야 진짜 실력을 알 수 있습니다.

**라벨 / 정답 (label, target)** — 입력에 대해 모델이 맞혀야 하는 값.

**device (CPU / GPU / cuda)** — 계산이 실행되는 장치. GPU는 행렬 연산을 대량으로 동시에 처리해 학습이 수십 배 빠릅니다. 모델과 데이터는 **같은 device**에 있어야 합니다.

## 머신러닝 기초

**머신러닝 (machine learning)** — 사람이 규칙을 짜는 대신, 데이터(예시)에서 컴퓨터가 규칙을 찾게 하는 방법. 딥러닝은 그중 신경망을 쓰는 분야입니다. ([첫 모델](#10-ml-first-model))

**특징 / 피처 (feature)** — 모델이 입력으로 보는 값들. 표 데이터에서는 열 하나가 특징 하나입니다.

**지도 학습 (supervised learning)** — (입력, 정답) 쌍으로 학습하는 방식. 분류와 회귀가 여기에 속합니다.

**비지도 학습 (unsupervised learning)** — 정답 없이 데이터의 구조를 찾는 방식. 군집화, 차원 축소 등.

**자기지도 학습 (self-supervised learning)** — 데이터 자체에서 정답을 만들어 내는 방식. LLM의 다음 토큰 예측, CLIP의 이미지-글 짝 맞추기가 대표적입니다.

**분류 (classification)** — 정해진 범주 중 하나를 맞히는 문제. LLM의 다음 토큰 예측도 분류입니다.

**회귀 (regression)** — 연속된 숫자를 맞히는 문제. 집값, 기온 예측 등. ([표 데이터와 트리](#12-ml-tabular-trees))

**scikit-learn** — 고전 머신러닝 알고리즘을 `fit` / `predict` / `score`라는 같은 사용법으로 제공하는 파이썬 라이브러리.

**kNN / k-최근접 이웃** — 학습 데이터 중 가장 비슷한 k개를 찾아 다수결로 답하는 모델. 학습되는 파라미터가 없습니다.

**선형 회귀 (linear regression)** — `예측 = 가중치 × 입력의 합 + 편향`. 가장 단순한 회귀 모델이자 신경망의 최소 단위.

**로지스틱 회귀 (logistic regression)** — 가중합으로 클래스별 점수를 내는 선형 **분류** 모델. 층이 하나인 신경망과 같습니다.

**혼동 행렬 (confusion matrix)** — 실제 클래스(행)와 예측 클래스(열)의 개수를 센 표. 무엇을 무엇으로 틀리는지 보여줍니다.

**정밀도 / 재현율 (precision / recall)** — 정밀도는 "모델이 양성이라 한 것 중 진짜 양성의 비율", 재현율은 "진짜 양성 중 모델이 찾아낸 비율". 불균형 데이터에서는 정확도보다 중요합니다.

**MAE / R²** — 회귀의 평가지표. MAE는 평균 절대 오차, R²는 1이면 완벽하고 0이면 평균으로 찍는 수준.

**기준선 / 베이스라인 (baseline)** — 비교 기준이 되는 가장 단순한 방법의 성능. 새 모델은 이보다 나아야 의미가 있습니다.

**검증 데이터 (validation set)** — 하이퍼파라미터·모델 선택과 학습 중단 시점을 정하는 데 쓰는 데이터. test와 구분됩니다. ([과적합과 검증](#11-ml-overfitting))

**교차검증 (cross-validation)** — 학습 데이터를 K등분해 돌아가며 검증용으로 쓰는 방법. 데이터가 적을 때 검증 결과를 안정시킵니다.

**결정 트리 (decision tree)** — "소득 > 5인가?" 같은 질문을 따라 내려가며 예측하는 모델. 제한 없이 키우면 과적합합니다.

**앙상블 (ensemble)** — 여러 모델의 예측을 합쳐 더 좋은 예측을 만드는 기법.

**랜덤 포레스트 (random forest)** — 데이터와 특징을 무작위로 달리한 트리 수백 그루의 평균. 과적합에 강합니다.

**그래디언트 부스팅 (gradient boosting)** — 트리를 하나씩 추가하며 이전까지의 오차를 보정해 나가는 앙상블. XGBoost, LightGBM이 이 계열이며 표 데이터에서 매우 강합니다.

**특징 공학 (feature engineering)** — 원본 데이터에서 모델에 유용한 특징을 사람이 만들어 넣는 작업. 딥러닝은 이를 모델이 스스로 하게 합니다.

**pandas / DataFrame** — 표 데이터를 다루는 파이썬 표준 도구와 그 자료형.

## 학습 과정

**순전파 (forward pass)** — 입력을 모델에 통과시켜 예측을 얻는 계산. `model(x)`.

**손실 / 로스 (loss)** — 예측이 정답에서 얼마나 틀렸는지를 나타내는 숫자 하나. 학습의 목표는 이것을 줄이는 것입니다. ([텐서와 자동미분](#20-tensor-autograd))

**손실 함수 (loss function)** — 손실을 계산하는 방법. 회귀엔 MSE, 분류와 언어모델엔 Cross Entropy가 표준입니다.

**MSE (평균제곱오차)** — (예측 − 정답)²의 평균. 숫자를 맞히는 문제에 씁니다.

**크로스 엔트로피 (cross entropy)** — 정답 클래스에 모델이 부여한 확률이 낮을수록 커지는 손실. 숫자 분류도, LLM의 다음 토큰 예측도 이것을 씁니다. ([첫 신경망](#21-mlp-mnist), [미니 GPT](#31-mini-gpt))

**기울기 / 그래디언트 (gradient)** — 각 파라미터를 조금 키웠을 때 손실이 얼마나 변하는지. 반대 방향으로 움직이면 손실이 줄어듭니다.

**역전파 (backpropagation, backward)** — 손실에서 출발해 모든 파라미터의 기울기를 한 번에 계산하는 알고리즘. `loss.backward()`.

**자동미분 (autograd)** — 계산 과정을 기록해 두었다가 미분을 자동으로 해 주는 PyTorch의 기능. ([텐서와 자동미분](#20-tensor-autograd))

**경사하강법 (gradient descent, SGD)** — `파라미터 -= 학습률 × 기울기`를 반복하는 학습 방법. SGD는 매번 데이터 일부(배치)만 써서 기울기를 구하는 방식입니다.

**옵티마이저 (optimizer)** — 기울기를 받아 파라미터를 실제로 갱신하는 규칙. SGD, Adam, AdamW 등.

**Adam / AdamW** — 파라미터마다 움직이는 크기를 자동 조절하는 옵티마이저. 대부분의 경우 기본 선택지입니다. AdamW는 weight decay를 올바르게 적용한 버전으로 LLM 학습의 표준입니다.

**학습률 (learning rate, lr)** — 한 스텝에 파라미터를 얼마나 움직일지. 너무 크면 발산, 너무 작으면 학습이 느립니다. 가장 중요한 하이퍼파라미터입니다. ([텐서와 자동미분](#20-tensor-autograd))

**배치 (batch) / 배치 크기** — 한 번의 갱신에 함께 쓰는 샘플 묶음과 그 개수. 크면 안정적이고 빠르지만 메모리를 많이 씁니다.

**스텝 (step, iteration)** — 파라미터를 한 번 갱신하는 것. 배치 하나 = 스텝 하나.

**에폭 (epoch)** — 학습 데이터 전체를 한 바퀴 도는 것.

**학습 루프 (training loop)** — `예측 → 손실 → backward → step`의 반복. 이 코스 전체를 관통하는 뼈대입니다. ([첫 신경망](#21-mlp-mnist))

**zero_grad** — 기울기를 0으로 초기화. PyTorch는 기울기를 누적하므로 매 스텝마다 해야 합니다.

**그래디언트 누적 (gradient accumulation)** — 작은 배치로 여러 번 backward한 뒤 한 번에 step해서, 메모리는 적게 쓰면서 큰 배치 효과를 내는 기법.

**loss 곡선** — 스텝에 따른 손실 그래프. 학습이 잘 되는지 보는 1순위 지표입니다.

**수렴 / 발산 (convergence / divergence)** — 손실이 안정적으로 낮아져 멈추는 것 / 오히려 커지거나 NaN이 되는 것. 발산하면 학습률부터 줄여 보세요.

**과적합 (overfitting)** — 학습 데이터만 외워서 새 데이터에는 약해지는 현상. train loss는 계속 내려가는데 val loss가 올라가면 과적합입니다. ([미니 GPT](#31-mini-gpt), [LLM 파인튜닝](#33-llm-finetune))

**과소적합 (underfitting)** — 모델이 너무 작거나 학습이 부족해 학습 데이터조차 못 맞히는 상태.

**일반화 (generalization)** — 본 적 없는 데이터에서도 잘 동작하는 능력. 학습의 진짜 목표입니다.

**정규화 기법 (regularization)** — 과적합을 줄이는 방법들의 총칭. dropout, weight decay, 데이터 증강 등.

**드롭아웃 (dropout)** — 학습 중 뉴런 일부를 무작위로 꺼서 과적합을 막는 기법. 평가 때는 꺼집니다(`model.eval()`).

**weight decay** — 가중치가 너무 커지지 않도록 조금씩 0 쪽으로 당기는 정규화.

**train / eval 모드** — `model.train()`과 `model.eval()`. dropout, batch norm처럼 학습 때와 평가 때 동작이 다른 층을 전환합니다.

**torch.no_grad** — 이 안에서는 기울기 계산용 기록을 하지 않습니다. 평가·추론 때 메모리와 시간을 아낍니다.

**체크포인트 (checkpoint)** — 학습 중간/최종의 가중치를 저장한 파일. ([CNN](#22-cnn))

**state_dict** — PyTorch 모델의 `이름 → 텐서` 사전. 저장·불러오기의 단위입니다. ([CNN](#22-cnn))

**시드 (seed)** — 난수 생성의 시작값. 고정하면 실험을 재현할 수 있습니다.

**정확도 (accuracy)** — 맞힌 비율. 분류 문제의 기본 평가지표.

**학습률 스케줄러 (lr scheduler)** — 학습 중 학습률을 계획에 따라 바꾸는 장치. ([학습 잘 시키는 법](#23-training-recipes))

**워밍업 / 코사인 감소 (warmup / cosine decay)** — 학습률을 처음엔 작게 시작해 빠르게 올리고(warmup), 이후 코사인 곡선을 따라 서서히 줄이는 표준 스케줄. LLM 학습 대부분이 사용합니다.

**조기 종료 (early stopping)** — 검증 성능이 더 나아지지 않으면 학습을 멈추거나, 가장 좋았던 시점의 가중치를 쓰는 것.

**전이학습 (transfer learning)** — 다른 데이터로 사전학습된 모델에서 출발해 내 과제에 맞게 학습하는 것. ([전이학습](#24-transfer-learning))

**사전학습 모델 (pretrained model)** — 대규모 데이터로 이미 학습된 가중치가 공개된 모델. ResNet, Qwen, CLIP 등.

**몸통 / 머리 (backbone / head)** — 특징을 뽑는 앞부분과, 그 특징으로 최종 답을 내는 마지막 층. 전이학습에서는 보통 머리를 새로 만듭니다.

**ImageNet** — 1000개 클래스, 120만 장의 이미지 데이터셋. 비전 모델 사전학습의 표준이었습니다.

**ResNet** — 잔차 연결을 도입해 매우 깊은 CNN의 학습을 가능하게 한 모델. 전이학습의 단골 몸통입니다.

**CUDA out of memory (OOM)** — GPU 메모리 부족 오류. 배치 크기·시퀀스 길이를 줄이거나 그래디언트 누적, LoRA, 양자화를 씁니다. ([내 프로젝트 시작하기](#50-your-project))

**혼합 정밀도 (mixed precision, fp16 / bf16)** — 32비트 대신 16비트 실수로 계산해 메모리와 시간을 절반으로 줄이는 기법. bf16이 fp16보다 안정적입니다.

## 신경망 구조

**층 / 레이어 (layer)** — 모델을 이루는 계산 단위. 층을 많이 쌓은 것이 "딥" 러닝입니다.

**nn.Module** — PyTorch에서 모델과 층을 정의하는 기본 클래스. `__init__`에 층을 선언하고 `forward`에 계산 순서를 적습니다. ([첫 신경망](#21-mlp-mnist))

**Linear / 완전연결층 (fully connected)** — `y = xW + b`. 가장 기본적인 층.

**활성화 함수 (activation)** — 층 사이에 넣는 비선형 함수. 이것이 없으면 층을 아무리 쌓아도 Linear 하나와 같습니다. ReLU, GELU 등.

**ReLU / GELU** — ReLU는 `max(0, x)`. GELU는 그것을 부드럽게 만든 것으로 트랜스포머에서 주로 씁니다.

**MLP (다층 퍼셉트론)** — Linear와 활성화 함수를 번갈아 쌓은 가장 기본적인 신경망. 트랜스포머 블록 안에도 들어 있습니다. ([첫 신경망](#21-mlp-mnist))

**은닉층 (hidden layer) / hidden size** — 입력과 출력 사이의 중간 층과 그 벡터 크기.

**로짓 (logit)** — softmax를 거치기 전의 클래스별 점수.

**소프트맥스 (softmax)** — 점수들을 합이 1인 확률로 바꾸는 함수.

**합성곱 / CNN (convolution)** — 작은 필터를 이미지 전체에 밀며 적용하는 층. 적은 파라미터로 위치에 상관없이 패턴을 찾습니다. ([CNN](#22-cnn))

**커널 / 필터 (kernel)** — 합성곱에서 밀고 다니는 작은 가중치 행렬(예: 3×3).

**채널 (channel)** — 이미지의 깊이 축. 입력은 RGB 3채널, CNN 안에서는 필터 개수만큼의 채널이 됩니다.

**패딩 / 스트라이드 (padding / stride)** — 가장자리에 0을 덧대는 것 / 필터를 몇 칸씩 이동할지.

**풀링 (pooling)** — 인접한 값들을 하나로 요약해 크기를 줄이는 연산. MaxPool은 최댓값을 취합니다.

**배치 정규화 / 레이어 정규화 (BatchNorm / LayerNorm)** — 층의 출력 분포를 고르게 맞춰 학습을 안정시키는 층. CNN은 BatchNorm, 트랜스포머는 LayerNorm(또는 RMSNorm)을 씁니다.

**잔차 연결 (residual / skip connection)** — `x + f(x)` 형태로 입력을 출력에 더해주는 구조. 깊은 모델의 학습을 가능하게 합니다. ([미니 GPT](#31-mini-gpt))

**임베딩 (embedding)** — 정수 번호(토큰 등)를 벡터로 바꾸는 표. 의미가 비슷하면 벡터도 가까워지도록 학습됩니다.

**인코더 / 디코더 (encoder / decoder)** — 입력을 벡터 표현으로 바꾸는 부분 / 표현에서 출력을 생성하는 부분. GPT류 LLM은 디코더만 있는 구조입니다.

## 트랜스포머와 LLM

**언어모델 / LLM (Large Language Model)** — 앞의 토큰들을 보고 다음 토큰의 확률을 예측하는 모델. 그것을 아주 크게 만든 것이 LLM입니다. ([미니 GPT](#31-mini-gpt))

**토큰 (token)** — 모델이 다루는 텍스트의 최소 단위. 글자, 단어 조각(서브워드), 단어일 수 있습니다.

**토크나이저 (tokenizer)** — 텍스트 ↔ 토큰 번호 목록을 변환하는 도구. 모델마다 짝이 정해져 있습니다. ([LLM 파인튜닝](#33-llm-finetune))

**BPE / 서브워드 (subword)** — 자주 나오는 글자 조합을 하나의 토큰으로 묶는 토큰화 방식. 실제 LLM 대부분이 사용합니다.

**어휘 (vocabulary, vocab size)** — 토크나이저가 아는 토큰의 전체 집합과 그 개수. LLM 출력층의 클래스 수이기도 합니다.

**다음 토큰 예측 (next token prediction)** — LLM의 유일한 학습 과제. 입력을 한 칸 민 것이 정답입니다.

**트랜스포머 (Transformer)** — 어텐션과 MLP 블록을 쌓은 구조. 현재 LLM·VLM의 사실상 표준입니다. ([미니 GPT](#31-mini-gpt))

**어텐션 / 셀프 어텐션 (self-attention)** — 각 토큰이 다른 토큰들을 얼마나 참고할지 스스로 계산해 정보를 모으는 장치.

**Query / Key / Value (Q, K, V)** — 어텐션의 세 벡터. Q(찾는 것)와 K(가진 것)의 내적으로 관련도를 구하고, 그 가중치로 V(전달할 정보)를 평균합니다.

**멀티헤드 어텐션 (multi-head)** — 어텐션을 여러 개(헤드)로 나눠 병렬로 수행. 헤드마다 다른 관계를 봅니다.

**causal mask (인과 마스크)** — 미래 토큰을 못 보게 가리는 마스크. GPT류가 "causal LM"이라 불리는 이유입니다.

**위치 임베딩 (positional embedding) / RoPE** — 어텐션은 순서를 모르므로 위치 정보를 따로 넣어줍니다. 최근 LLM은 회전식 위치 인코딩(RoPE)을 씁니다.

**컨텍스트 길이 (context length, block size)** — 모델이 한 번에 볼 수 있는 최대 토큰 수.

**lm_head** — 마지막 벡터를 어휘 크기의 로짓으로 바꾸는 출력층.

**생성 (generation) / 자기회귀 (autoregressive)** — 토큰을 하나 예측해 입력 뒤에 붙이고, 다시 예측하기를 반복하는 방식.

**샘플링 / temperature / top-p / greedy** — 다음 토큰을 고르는 방법. greedy는 항상 1등(`do_sample=False`), 샘플링은 확률대로 뽑기. temperature가 높을수록 다양하고 엉뚱해지며, top-p는 상위 확률 후보 안에서만 뽑습니다.

**KV 캐시** — 생성 시 이전 토큰들의 K, V를 저장해 두어 재계산을 피하는 최적화.

**FlashAttention / SDPA** — 어텐션을 메모리 효율적으로 계산하는 최적화된 구현. PyTorch에서는 `F.scaled_dot_product_attention`.

**사전학습 (pre-training)** — 방대한 텍스트로 다음 토큰 예측을 학습하는 단계. 모델의 지식 대부분이 여기서 옵니다.

**베이스 모델 vs Instruct(Chat) 모델** — 사전학습만 한 모델은 글을 이어 쓸 뿐입니다. 거기에 지시를 따르도록 추가 학습한 것이 Instruct 모델입니다.

**파인튜닝 (fine-tuning)** — 사전학습된 모델을 내 데이터로 추가 학습시켜 원하는 행동·형식·도메인에 맞추는 것. ([LLM 파인튜닝](#33-llm-finetune))

**SFT (Supervised Fine-Tuning)** — (질문, 모범 답) 쌍으로 하는 파인튜닝. ([LLM 파인튜닝](#33-llm-finetune), [VLM 파인튜닝](#42-vlm-finetune))

**선호 학습 (preference learning)** — "두 답 중 어느 쪽이 나은가"라는 비교 데이터로 모델을 다듬는 단계. SFT 다음에 옵니다. ([선호 학습 DPO](#35-dpo))

**RLHF** — 선호 데이터로 보상 모델을 학습시킨 뒤, 강화학습으로 LLM이 높은 보상을 받도록 학습하는 방법.

**DPO (Direct Preference Optimization)** — 보상 모델과 강화학습 없이 선호 데이터에서 바로 LLM을 학습하는 방법. chosen의 확률은 올리고 rejected의 확률은 내립니다.

**chosen / rejected** — 선호 데이터에서 같은 질문에 대한 "선호하는 답"과 "선호하지 않는 답".

**reference 모델** — 선호 학습에서 기준점이 되는 학습 전 모델. 여기서 너무 멀어지지 않도록 제약해 모델이 망가지는 것을 막습니다.

**보상 모델 (reward model)** — 답변에 점수를 매기도록 학습된 모델. RLHF에서 사람의 선호를 대신합니다.

**로그 확률 (log probability)** — 확률에 로그를 취한 값. 문장의 로그 확률은 각 토큰 로그 확률의 **합**입니다.

**프롬프트 (prompt)** — 모델에 주는 입력 텍스트. 가중치를 바꾸지 않고 행동을 바꾸는 가장 싼 수단입니다. ([LLM 다루기](#32-llm-inference))

**시스템 프롬프트 (system prompt)** — 대화 맨 앞에서 모델의 역할과 규칙을 정하는 메시지.

**퓨샷 / 제로샷 (few-shot / zero-shot)** — 프롬프트에 예시를 몇 개 넣는 것 / 예시 없이 지시만 하는 것. 학습 없이 새 과제를 시키는 방법입니다.

**인컨텍스트 러닝 (in-context learning)** — 가중치 변화 없이, 프롬프트 안의 예시에서 패턴을 읽어 따라 하는 LLM의 능력.

**top-k** — 확률 상위 k개 토큰 안에서만 샘플링하는 방법.

**RAG (검색 증강 생성)** — 질문과 관련된 문서를 검색해 프롬프트에 넣고 LLM이 그것을 읽고 답하게 하는 방법. 모델이 모르는 지식을 넣는 기본 수단입니다. ([임베딩과 RAG](#34-embeddings-rag))

**임베딩 모델 (embedding model)** — 문장(또는 이미지)을 의미를 담은 벡터 하나로 바꾸는 모델. 검색·분류·군집화에 씁니다.

**코사인 유사도 (cosine similarity)** — 두 벡터의 방향이 얼마나 같은지(−1~1). 길이를 1로 정규화하면 내적과 같습니다.

**의미 검색 (semantic search)** — 키워드 일치가 아니라 임베딩 벡터의 가까움으로 찾는 검색.

**벡터 DB (vector database)** — 임베딩 벡터를 저장하고 가까운 벡터를 빠르게 찾아 주는 저장소. FAISS, Chroma, pgvector 등.

**청킹 (chunking)** — 긴 문서를 검색 단위로 잘게 나누는 것.

**리랭킹 (reranking)** — 1차 검색 결과를 더 정밀한 모델로 다시 순위 매기는 것.

**Trainer / TRL** — Hugging Face의 학습 루프 도구와, LLM SFT·선호 학습 전용 라이브러리(`SFTTrainer`, `DPOTrainer`).

**채팅 템플릿 (chat template)** — 대화를 모델이 학습한 특수 토큰 형식의 텍스트로 바꾸는 규칙. 학습과 추론에서 같은 템플릿을 써야 합니다.

**특수 토큰 / EOS / PAD** — 역할이 정해진 토큰. EOS는 "여기서 끝", PAD는 길이를 맞추기 위한 채움용입니다.

**패딩 / 어텐션 마스크 (padding / attention mask)** — 길이가 다른 문장을 한 배치로 묶으려고 빈자리를 채우는 것 / 그 빈자리를 모델이 무시하도록 알려주는 0·1 표시.

**라벨 마스킹 (-100)** — 라벨이 `-100`인 위치는 손실 계산에서 제외됩니다. 질문 부분은 가리고 답 부분만 학습시킬 때 씁니다. ([LLM 파인튜닝](#33-llm-finetune))

**freeze (얼리기)** — `requires_grad=False`로 파라미터를 학습에서 제외하는 것. ([CNN](#22-cnn))

**전체 파인튜닝 (full fine-tuning)** — 모든 파라미터를 학습. 성능 여지는 크지만 메모리가 많이 들고 데이터가 적으면 모델이 망가지기 쉽습니다.

**PEFT (Parameter-Efficient Fine-Tuning)** — 일부 파라미터만 학습하는 기법들의 총칭이자 Hugging Face 라이브러리 이름. LoRA가 대표적입니다.

**LoRA** — 원래 가중치 `W`는 얼리고 작은 행렬 `A`, `B`만 학습해 `W + BA`로 쓰는 기법. 전체의 1% 미만만 학습합니다. ([LLM 파인튜닝](#33-llm-finetune))

**rank (r) / lora_alpha / target_modules** — LoRA의 설정값. r은 작은 행렬의 크기(클수록 표현력↑), alpha는 LoRA 출력의 배율, target_modules는 LoRA를 붙일 층의 이름입니다.

**어댑터 (adapter) / merge** — LoRA로 학습된 작은 가중치 파일. `merge_and_unload()`로 원래 모델에 합칠 수 있습니다.

**양자화 (quantization) / QLoRA** — 가중치를 8비트·4비트로 줄여 메모리를 아끼는 기법. QLoRA는 4비트로 줄인 모델 위에 LoRA를 학습합니다.

**catastrophic forgetting (파국적 망각)** — 새 과제를 학습하다가 원래 능력을 잃는 현상. ([VLM 파인튜닝](#42-vlm-finetune))

**환각 (hallucination)** — 모델이 사실이 아닌 내용을 그럴듯하게 지어내는 현상.

**Hugging Face / Hub / transformers** — 모델·데이터셋 공유 플랫폼(Hub)과, 그 모델들을 몇 줄로 불러 쓰는 라이브러리(transformers). ([LLM 파인튜닝](#33-llm-finetune))

**safetensors** — 모델 가중치를 저장하는 표준 파일 형식. 본질은 state_dict와 같습니다.

## 비전과 VLM

**VLM (Vision-Language Model)** — 이미지와 텍스트를 함께 입력받아 텍스트로 답하는 모델. 보통 이미지 인코더 + 프로젝터 + LLM 구조입니다. ([미니 VLM](#41-mini-vlm))

**멀티모달 (multimodal)** — 텍스트, 이미지, 음성 등 여러 종류의 입력을 다루는 것.

**이미지 인코더 / 비전 인코더 (vision encoder)** — 이미지를 벡터들의 나열로 바꾸는 모델. VLM의 "눈".

**ViT (Vision Transformer)** — 이미지를 패치로 잘라 각 패치를 토큰처럼 다루는 트랜스포머.

**패치 (patch)** — 이미지를 격자로 자른 조각(예: 32×32 픽셀). ViT의 토큰에 해당합니다.

**CLS 토큰** — 시퀀스 맨 앞에 붙여 전체를 요약하도록 학습시키는 특수 토큰.

**CLIP / SigLIP** — 이미지와 그 설명 문장의 벡터가 가까워지도록 대조 학습한 모델. 이들의 이미지 인코더가 VLM의 눈으로 널리 쓰입니다.

**대조 학습 (contrastive learning)** — 짝이 맞는 쌍은 가깝게, 안 맞는 쌍은 멀게 만드는 학습 방식. 손실은 "배치 안에서 내 짝 고르기" cross entropy입니다. ([CLIP](#40-clip))

**제로샷 분류 (zero-shot classification)** — 클래스 이름을 글로 써 주고, 이미지 벡터와 가장 가까운 글을 고르는 방식. 추가 학습 없이 임의의 클래스를 분류합니다.

**프로젝터 / 커넥터 (projector / connector)** — 이미지 인코더의 출력을 LLM의 임베딩 공간으로 변환하는 작은 신경망. VLM의 "통역사". ([미니 VLM](#41-mini-vlm))

**이미지 토큰 (image token)** — 프로젝터를 거쳐 LLM 입력에 끼워 넣는 이미지 벡터들. LLM은 이를 단어처럼 읽습니다.

**inputs_embeds** — 토큰 번호 대신 임베딩 벡터를 LLM에 직접 넣는 입력 방식. 이미지 토큰을 끼워 넣을 때 씁니다.

**프로세서 (processor)** — 토크나이저와 이미지 전처리기(리사이즈·정규화)를 묶은 Hugging Face 객체. ([VLM 파인튜닝](#42-vlm-finetune))

**이미지 분할 (image splitting, tiling)** — 고해상도 이미지를 여러 조각으로 나눠 각각 인코딩하는 기법. 세밀해지지만 이미지 토큰 수가 크게 늘어납니다.

**LLaVA** — "CLIP 인코더 + 프로젝터 + LLM" 구조를 대중화한 대표적인 오픈 VLM. 1단계에서 프로젝터만, 2단계에서 LLM까지 함께 학습합니다.

**캡셔닝 / VQA (captioning / Visual Question Answering)** — 이미지를 설명하는 과제 / 이미지에 대한 질문에 답하는 과제.

**데이터 증강 (data augmentation)** — 이미지를 뒤집고 자르고 색을 바꿔 학습 데이터를 늘리는 기법.
